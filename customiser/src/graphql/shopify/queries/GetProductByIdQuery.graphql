query ShopifyGetProductById($id: ID!) {
  product(id: $id) {
    title
    variants(first: 100) {
      nodes {
        id
        title
        sku
        price {
          amount
          currencyCode
        }
      }
    }
  }
}
